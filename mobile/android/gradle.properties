org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Disable Kotlin incremental compilation — required because the Pub cache (C:\)
# and the project (D:\) are on different drive roots, which breaks Kotlin's
# relative-path resolution in its incremental storage.
kotlin.incremental=false
